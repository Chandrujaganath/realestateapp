import { URISchemeHandler, URIComponents } from '../uri';
export interface WSComponents extends URIComponents {
  resourceName?: string;
  secure?: boolean;
}
declare const _handler: URISchemeHandler;
export default handler;
